services:
  - type: web
    name: skin-disease-app-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && pip install -r requirements.txt
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
